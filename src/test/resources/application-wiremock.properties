wiremock.reset-mappings-after-each-test=true
wiremock.port=9000
notification.serviceUrl=http://localhost:${wiremock.port}/notification_service
oauth.serviceUrl=http://localhost:${wiremock.port}/identity
test-log-level=DEBUG
logging.level.root=${test-log-level}
logging.level.uk.gov.cabinetoffice.csl.client.HttpClient=${test-log-level}
logging.level.uk.gov.cabinetoffice.csl.client=${test-log-level}
logging.level.org.springframework=${test-log-level}
