
spring:
  jackson:
    serialization:
      write_dates_as_timestamps: false
    default-property-inclusion: non_null
  resources:
    add-mappings: false

server:
  port: 9004

oauth:
  clientId: ${CLIENT_ID:9fbd4ae2-2db3-44c7-9544-88e80255b56e}
  clientSecret: ${CLIENT_SECRET:test}
  checkTokenEndpointUrl: ${CHECK_TOKEN_URL:http://localhost:8080/oauth/check_token}

---

spring:
  profiles: production
